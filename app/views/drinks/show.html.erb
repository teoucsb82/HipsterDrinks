<div class="container">
	<h1><%= @drink.name %></h1>
	<p>
		<%= @drink.description %>
	</p>
	<p>
		Created by: <%= User.find(@drink.user_id).email %>
	</p>

	<p>photo</p>
	<%= filepicker_image_tag @drink.filepicker_url, w: 160, h: 160, fit: 'clip' %>

	<%= link_to "back", drinks_url %>
</div>