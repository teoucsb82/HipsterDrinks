<%= form_for(@drink.favorites.find_by(favoritor_id: current_user.id),
             html: { method: :delete }) do |f| %>
  <%= f.submit "Ø Remove from Favorites", class: "btn btn-sm btn-danger" %>
<% end %>